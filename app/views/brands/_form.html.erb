<%= form_with(model: brand) do |form| %>
  <% if brand.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
        <% brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>
  <Br />
  <%= form.select :product_id, Product.all.collect { |p| [ p.title, p.id ] },  { prompt: "Select product" } ,include_blank: true %>
  
  <div>
  <Br />
    <%= form.submit %>
  </div>
<% end %>
