<%= form_with(model: brandproduct) do |form| %>
  <% if brandproduct.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(brandproduct.errors.count, "error") %> prohibited this brandproduct from being saved:</h2>

      <ul>
        <% brandproduct.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
